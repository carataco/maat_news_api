package models

/*
Role: Define data structures used across layers.

Responsibilities:

Pagination metadata (limit, offset, has_more).

Potentially, other shared response models (e.g., country struct).

Why: Avoids ad-hoc map usage everywhere and ensures consistent JSON responses.
*/
